[project]
name = "semweb-agent"
version = "0.1.0"
description = "Tutorial agent: Together AI reasoning over the semantic-web MCP server"
requires-python = ">=3.10"
dependencies = [
    "together>=2.0.0",
    "requests>=2.31",
]